---
import { STATS } from '../../consts';
---

<section class="py-12 border-y border-border bg-bg-surface/50 backdrop-blur-sm">
  <div class="max-w-7xl mx-auto px-4">
    <div class="grid grid-cols-2 md:grid-cols-4 gap-8 md:gap-12">
      {
        STATS.map((stat) => (
          <div class="text-center group cursor-default">
            <h3 class="text-3xl md:text-4xl font-bold text-text-bright mb-2 group-hover:text-accent transition-colors">
              {stat.prefix && (
                <span class="text-lg md:text-xl font-medium mr-1 opacity-70">{stat.prefix}</span>
              )}
              {stat.value}
              {stat.suffix && (
                <span class="text-lg md:text-xl font-medium ml-1 opacity-70">{stat.suffix}</span>
              )}
            </h3>
            <p class="font-code text-xs md:text-sm text-text-muted uppercase tracking-wider">
              {stat.label}
            </p>
          </div>
        ))
      }
    </div>
  </div>
</section>
